#if LAB >= 4 */

OBJDIRS += user/simple
CLEAN_FILES += user/simple/simple

SIMPLE_OBJS := $(addprefix user/simple/, entry.o libos.o simple.o)

user/simple/simple: $(SIMPLE_OBJS)
	$(LD) -o $@ $(LDFLAGS) -nostdlib $^

#endif /* LAB >= 4 */
